global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/prometheus-rules.yml

scrape_configs:
  - job_name: impitrack-otel-collector
    static_configs:
      - targets: ["otel-collector:9464"]
